[workspace]

[package]
name = "bot"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wectr-sdk = { path = "../../crates/wectr-sdk", features = ["guest"] }

[profile.release]
lto = true
strip = true
opt-level = "z"
codegen-units = 1
